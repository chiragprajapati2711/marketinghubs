import PricingClient from './PageClient';

export const metadata = {
  alternates: {
    canonical: 'https://www.marketinghubs.com/pricing/',
  },
};

export default function PricingPage() {
  return <PricingClient />;
}
