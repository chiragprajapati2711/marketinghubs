import AboutClient from './PageClient';

export const metadata = {
  alternates: {
    canonical: 'https://www.marketinghubs.com/about/',
  },
};

export default function AboutPage() {
  return <AboutClient />;
}
