import ServicesClient from './PageClient';

export const metadata = {
  alternates: {
    canonical: 'https://www.marketinghubs.com/services/',
  },
};

export default function ServicesPage() {
  return <ServicesClient />;
}
