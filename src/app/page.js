import HomeClient from './HomeClient';

export const metadata = {
  alternates: {
    canonical: 'https://www.marketinghubs.com/',
  },
};

export default function Home() {
  return <HomeClient />;
}
