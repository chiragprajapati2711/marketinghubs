import ContactClient from './PageClient';

export const metadata = {
  alternates: {
    canonical: 'https://www.marketinghubs.com/contact/',
  },
};

export default function ContactPage() {
  return <ContactClient />;
}
